
(library
  (name reimplementation)
  (modules reimplementation)
  (libraries
    core))

(library
  (name divisors)
  (modules divisors)
  (libraries
    core))

(library
  (name towers)
  (modules towers)
  (libraries
    core))